- Clone in ~/.local/share/icons/ or ~/.icons
- build using 
```
hyprcursor-util --create crosscode/
```
- use with `hyprctl setcursor Crosscode-Cursor [SIZE]` or control with `HYPRCURSOR_THEME` and `HYPRCURSOR_SIZE` environement variables
