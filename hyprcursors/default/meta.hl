# Size 0 due to SVG
define_size = 0, default.svg

# Use none due to SVG
resize_algorithm = none

# Clickpoint: top-left
hotspot_x = 0.04
hotspot_y = 0.04

# Override
define_override = default
define_override = left_ptr
