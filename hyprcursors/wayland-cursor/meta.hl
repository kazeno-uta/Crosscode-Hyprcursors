# Size 0 due to SVG
define_size = 0, wayland-cursor.svg

# Use none due to SVG
resize_algorithm = none

# Clickpoint: center
hotspot_x = 0.50
hotspot_y = 0.50

# Name
define_override = wayland-cursor
define_override = Wayland_cursor
